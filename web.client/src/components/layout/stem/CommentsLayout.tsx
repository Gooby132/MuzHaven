import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
`

type Props = {}

export const CommentsLayout = (props: Props) => {
  return (
    <Container>CommentsLayout</Container>
  )
}